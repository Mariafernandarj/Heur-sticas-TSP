package main

import (
	"fmt"
	"log"
	"tsp-rs/internal/data"
)

func main() {
	db, err := data.InicioDB("tsp.sql")
	if err != nil {
		log.Fatal("Error iniciando la BD:", err)
	}
	defer db.Close()

	if err := data.RecibirArchivo(); err != nil {
		log.Fatalf("Error en la ejecución: %v", err)
	}

}
